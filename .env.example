# Choose llm to use Claude, or deterministic for the local rules fallback.
SDOC_CLASSIFIER=llm

# Add your Anthropic API key locally. Never commit the real key.
ANTHROPIC_API_KEY=replace-with-your-anthropic-api-key
